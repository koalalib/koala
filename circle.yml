dependencies:
  pre:
    - cd extsrc/googletest/; mkdir build; cd build; cmake ..; make
  override:
    - cd test/make; make
test:
  override:
    - cd test/make; ./assocarray_unittest; ./assocmatrix_unittest; ./assocTabConstInterface_unittest
checkout:
  post:
    - git submodule init
    - git submodule update
